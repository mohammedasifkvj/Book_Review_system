export class Book {
    constructor({ id, title, author, genre, description}) {
      this.id = id;
      this.title = title;
      this.author = author;
      this.genre = genre;
      this.description = description;
    }
  }